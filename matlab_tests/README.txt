To run these tests:

0) Using CMake and Visual Studio, compile all the C/C++ code, on the "Release" setting.
1) Start Matlab.
2) In Matlab, cd into this directory.
3) Type "run_tests" on the Matlab command line and hit the enter key.
